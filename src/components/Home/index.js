/* eslint-disable no-unused-vars*/
import React, { Component } from 'react';
/* eslint-enable no-unused-vars*/

const Home = () => (
  <div style={ { width: '100%' } }>
    <h1 style={ { maxWidth: '20%', textAlign: 'center', margin: '0 auto' } }>
      {'React With Hotness!'}
    </h1>
  </div>
);

export default Home;
